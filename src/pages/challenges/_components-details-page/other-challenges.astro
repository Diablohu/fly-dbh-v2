---
import { type ChallengeItemType } from "@/types";

import InfoBlock from "./_info-block.astro";
import ChallengeItem from "@/components/challenge-item";

type Props = {
    list: ChallengeItemType["other_challenges_this_aerodrome"];
} & astroHTML.JSX.HTMLAttributes;

const { list, ...props } = Astro.props;
---

{
    Array.isArray(list) && list.length > 0 && (
        <InfoBlock {...props} title="本机场的其他挑战" emoji="🛬">
            <div class="list">
                {list.map((item) => (
                    <ChallengeItem item={item} showAircraftTypes />
                ))}
            </div>
        </InfoBlock>
    )
}

<style lang="less">
    @import "../_mixins.less";

    .list {
        .challenge-list-styles();
    }
</style>

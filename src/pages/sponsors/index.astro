---
// TODO: Sponser Index Page
// There is only one sponsor right now, so we redirect to the sponsor page directly.

import { getSponsorPageLink as _getSponsorPageLink } from "@/global";
return Astro.redirect(_getSponsorPageLink("vhf123.45"), 301);
---

---
// TODO: Sponser Index Page
// There is only one partner right now, so we redirect to the partner page directly.

import { getPartnerPageLink as _getPartnerPageLink } from "@/global";
return Astro.redirect(_getPartnerPageLink("vhf123.45"), 301);
---
